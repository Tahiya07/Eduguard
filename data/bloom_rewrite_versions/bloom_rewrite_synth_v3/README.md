# Bloom rewrite synth v3 (placeholder)

This directory is populated by:

```powershell
python experiments/bloom_rewrite/scripts/prepare_bloom_rewrite_synth_v3.py
```

Do not overwrite `bloom_rewrite_synth_v1` or `data/bloom_rewrite` (v2).
